# Assorted Docker Containers

This project has now been split into separate repositories to make it easier to
version the underlying PHP runtime.

Please update any references to use:

 * somnambulist/php-fpm:7.X-latest
 * somnambulist/php-ppm:7.X-latest

Each set of build files is now in a separate repository:

 * https://github.com/dave-redfern/docker-php-fpm
 * https://github.com/dave-redfern/docker-php-ppm

